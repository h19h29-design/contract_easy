# 계약 문서 작성 자료 대조 — 2026-09-12

## 목적과 현재 판정

사용자는 계약 문서를 쉽게 작성하는 기능을 최우선으로 진행하도록 지시했다.
검색·업무관리 기능의 완료는 계약서 작성 기능의 완료가 아니다.
공사표준계약서·착공계·준공계·대금청구서 4종의 선택·공통정보 재사용·비공개 초안 저장/수정·항목 미리보기·HWPX 다운로드를 구현했다.
일괄 생성과 실제 한글 열기/편집/인쇄 검증은 남아 있다. 한글 직접 검증은 사용자 보류이며 정식 사용 완료로 간주하지 않는다.
OCR·RAG·AI 답변·NAS SSH 변경은 이번 작성 기능 범위에서 제외한다.

## 원본 위치와 대조 결과

- Mac `data/raw/attachments`와 `data/normalized/markdown`은 비어 있다. Git으로 이전된 검색 청크는 11,259개이나 `type=form`은 0개다.
- `wiki/generated/12-서식-목록과-사용법.md`는 대응하는 수집 자료가 없다고 표시한다. 위키 16파일 존재만으로 서식 안내 완성을 판단할 수 없다.
- Windows `D:\opencode\sen-contract-rag-blueprint\data\raw\attachments`에 원본·추출본이 남아 있다.
- Windows 재귀 파일 실측: ZIP 62, HWP 72, PDF 23, XLS 14, XLSX 6, TXT 70, PPTX 1, PNG 1, `.p` 1, 확장자 없음 1. 과거 다운로드 건수와는 집계 단위가 다르며 추출본을 포함한다.
- 기존 공개 수집 자료를 확인한 결과다. 사용자가 별도로 제공한 모든 자료의 누락 여부까지 검증한 것은 아니다.

## 첫 작성 대상 후보의 출처

- 원본 ZIP: `D:\opencode\sen-contract-rag-blueprint\data\raw\attachments\28\28818aeb577853c3-원클릭_프로그램_2026.5.수정_.zip`
- SHA-256: `28818aeb577853c3d0801b00d3402dc12dfcd0f600b39a128ae756f4ebc9e429`
- 크기: 8,117,760 bytes (`data/manifests/attachments.jsonl` 기록)
- 수집 기록: 2026-08-26T08:25:15.473Z, 성공. Windows 원본 해시가 manifest와 일치했다.
- 기록된 출처: `https://buseo.sen.go.kr/component/file/ND_fileDownload.do?q_fileSn=2261634&q_fileId=7a26d3c9-edd7-4658-be7f-532a4ee444a5`
- 선택 검토한 내부 파일: `1. 공사서류 원클릭 프로그램(2026.4.)-서울시교육청용.xlsm` (2,590,914 bytes).
- 동일 ZIP에 타기관용 공사서류, 서울시교육청용/타기관용 설계용역 서류, 설계용역비 산출 파일이 있다.
- 파일명은 배포본 표기이며, 현재 법령에 적합한 최신 서식이라는 보증은 아니다. 외부 재수집은 실행하지 않았다.

## 매크로 실행 없는 구조 확인

ZIP과 내부 XLSM의 XML만 메모리에서 읽었다. Excel·VBA·외부 연결·수식 계산은 실행하지 않았다.

| 대상 | 확인 결과 |
| --- | --- |
| `2.데이터입력` | `A1:P46`, 수식 3개, 병합 영역 4개 |
| `3.공사표준계약서` | `B1:AF60`, 수식 29개, 병합 영역 60개 |
| VBA | `xl/vbaProject.bin` 존재. 실행·안전성 검증 안 함 |
| 후속 서식 | 사용인감계, 수의계약 통합서약서, 안전 체크리스트, 착공계, 현장대리인계, 공정표, 준공계, 준공검사원, 대금청구서, 하자 관련 서식 등 |
| 숨김 시트 | `(삭제)`로 표시된 과거 서식이 존재. 현행 후보 목록에 자동 편입 금지 |

확인한 계약서 항목은 계약상대자, 상호·주소·대표자, 계약보증금, 지체상금률,
착공·준공일, 하자보증금율·금액·책임기간 등이다. 입력 셀과 결과 셀의 전체 대응,
인쇄 배치, 매크로 역할과 재사용 조건은 후속 분석이 필요하다.

## 제안하는 구현 범위

1. 첫 대상은 원본 `공사표준계약서`. 기존 로그인·프로젝트 소유권 검사를 재사용한다.
2. 공사·기관·업체 공통정보를 입력하고, 문서별 항목과 초안을 비공개 프로젝트에 저장한다.
3. 원본에서 확인한 항목을 채워 미리보기·수정·다운로드를 제공한다. **사용자가 HWPX 필수를 확정했다.** 엑셀/PDF/DOCX로 대체해 완료 처리하지 않는다.
4. 사용자가 입력한 계약금액·날짜와 법적 판단값을 구분한다. 기존 XLSM의 비율·기간 수식이나 고정값을 승인 없이 서비스 규칙으로 가져오지 않는다.
5. 검증은 입력 누락/날짜/금액 문자열 보존, 사용자 간 접근 차단, 초안 재조회, 원본 대비 필드와 출력 배치, 브라우저 작성→다운로드 흐름을 포함한다.

사용자가 아래 HWPX 작성 흐름을 승인해 `codex/contract-hwpx`에서 구현했다.
원본 전체 배포·Git 추가, 매크로 실행, 운영 DB 변경은 하지 않는다.

## 승인된 HWPX 작성 흐름

- 기존 프로젝트 상세에서 `계약서 작성`으로 진입한다. 첫 버전은 공사표준계약서 한 종류에 집중한다.
- 기관·업체·공사·계약정보 입력, 비공개 초안 저장/다시 열기, 항목 수정, 미리보기, `.hwpx` 다운로드를 연결한다.
- 화면에서 입력 항목을 수정하며 자유 배치 편집기는 만들지 않는다. 다운로드 후 한글에서 본문과 표를 편집할 수 있어야 한다.
- 원본에 있는 항목·문구·표 구성을 대조한 전용 HWPX 서식을 서버에서 채우는 방식을 권장한다. 원본 재사용 조건을 확인하고 XLSM→HWPX의 임의 일괄 변환은 하지 않는다.
- 대안인 Windows/Excel/한글 상시 자동화 방식은 설치·라이선스·실행 세션에 대한 운영 의존성이 생긴다. 첫 버전의 런타임에는 포함하지 않는다.
- 법적 비율·기간·계약방법을 추정하지 않는다. 사용자 입력값과 승인 규칙에서 나온 값을 구분하고, 미확정 항목을 자동 채우지 않는다. 검토 필요 표기와 다운로드 시 확인 절차를 둔다.
- 프로젝트 권한과 CSRF 검사를 재사용한다. 원본 자료와 개인별 작성 문서를 분리하며 작성 내용은 공개 corpus나 로그에 넣지 않는다.
- 생성 파일은 XML/ZIP 기반 실제 HWPX여야 하며 본문을 이미지로 넣거나 다른 문서의 확장자만 바꾸지 않는다. 원본 매크로·외부 연결·실제 인감 이미지는 포함하지 않는다.
- 웹 미리보기는 항목 확인용이며 한글의 실제 페이지 나눔과 동일하다고 보장하지 않는다. 한글에서 표·문단·긴 입력값·페이지 나눔을 별도로 검증한다.

## HWPX 검증 근거와 환경 확인

- 한컴 공식 포맷 설명: https://tech.hancom.com/hwpxformat/ (2026-09-12 확인).
  HWPX의 ZIP/XML 구조, 본문/서식 파일과 패키지 참조 구조를 확인했다. 이 개요만으로 생성기의 포맷 적합성을 보증하지 않는다.
- Mac `/Applications`, `/Users/mac-mini/Applications`의 2단계 앱 폴더 및 Windows `C:\Program Files\Hnc`, `C:\Program Files (x86)\Hnc` 아래에서 한글 앱/Hwp.exe를 찾지 못했다. 다른 경로에 설치돼 있지 않다는 의미는 아니다.
- Windows HKLM/HKCU Uninstall 등록정보도 읽기 전용 확인했으나 Hancom/Hnc/한컴/한글 이름의 항목이 없었다. 최초 SSH 인용 오류는 EncodedCommand로 수정했으며 정상 실행에서도 일치 항목이 없었다.
- 자동 검증: 패키지/XML 참조, 모든 입력값 왕복 추출, 특수문자·긴 한글·줄바꿈·누락 처리, 권한/API/DB 및 브라우저 다운로드 회귀.
- 실제 한글 검증: 파일 열기, 계약서 표·문구·인쇄 배치 확인, 수정 후 HWPX 재저장·재열기. 실제 한글 환경이 확인되지 않으면 이 항목은 미검증으로 보고하고 사용 가능 완료로 표시하지 않는다.

## 2026-09-12 구현 및 대조 범위

- `packages/shared/src/contract.ts`의 33개 입력 항목을 원본 `xl/worksheets/sheet3.xml`의 항목명과 대조했다. 기존 XLSM 수식·캐시 금액·비율을 실행하거나 복사하지 않았다.
- 매핑: 계약/공고번호 → 문서 머리말; 발주처·업체·연대보증인 → 당사자 표; 공사명·현장·금액·보증금·지체율·조정방법·착공/준공일 → 계약내용 표; 기타사항 → 문단; 공종·금액·보증율/보증금·책임기간 → 하자담보 표; 계약일·기관/업체 서명정보 → 끝부분.
- 원본 C27 계약 체결 문구를 보존했다. 붙임은 사용자 입력 목록만 출력하며 원본의 5개 기본 붙임을 자동으로 존재한다고 표시하지 않는다.
- HWPX는 편집 가능한 텍스트/표로 구성했다. 첫 버전의 하자담보 표는 공종 1행이며 추가 행은 한글에서 편집해야 한다. 원본 Excel 병합/인쇄 배치의 완전 복제는 아니다.
- 원본 sharedStrings의 저작권/재배포 관련 제한 문구 검색은 일치 항목이 없었다. 이는 재배포 허가 확인이 아니다. 원본 전체는 포함하지 않았으며 외부 공개 전 재사용 조건 확인이 필요하다.
- 빈 HWPX 기반은 Apache-2.0 `airmang/python-hwpx`의 고정 커밋에서 가져왔다. 원본·라이선스·NOTICE·수정 설명은 `apps/api/assets/hwpx/`에 보관한다.
- 비공개 초안은 FileStore/PgStore 별도 revision으로 저장하며 기존 버전을 덮어쓰지 않는다. FileStore 저장 파일은 600으로 제한한다. 개인 입력값은 공개 검색·감사 detail에 넣지 않는다.
- 검증 결과는 `TEST_RESULTS.md`. NAS/운영 DB/SSH 설정 변경과 자동 push는 하지 않았다.

## 후속 서식 대조 — T-215

### 추가 대조 T-216 (2026-09-12)

- `16.현장대리인계` / `xl/worksheets/sheet20.xml`: D6 공사명(C9), D8~G8 예정 착공일(C29)~준공기한(C31), B11:I13 구분/종별/기술경력(등급)/면허번호/생년월일/성명/비고, H13 성명(C23). B15 제출 문구를 그대로 사용한다. B17 건설기술자수첩 사본은 자동 첨부했다고 표시하지 않고 사용자가 목록을 입력한다. 제출일은 원본 C30 자동 연결 대신 별도 직접 입력. 회사/주소/대표와 명시 수신인 사용. 생년월일·면허는 해당 서식에만 출력하고 필수 개인정보 입력을 강제하지 않는다.
- `17.공정표` / `xl/worksheets/sheet21.xml`: C4 공사명, C6 계약금액, C8 계약일, C10 실제 착공일, C12 준공기한. B15:S21 공정명과 날짜별 막대 예시. C15는 예정일 월(C29), C16은 실제일 일(C30)을 섞고 2월28일 고정의 날짜 수식이 있어 그대로 복제하지 않는다. 사용자 입력 `공정 | 시작일 | 종료일`을 최대30행 기간 표로 재구성하고 별도 제출일을 입력한다. 원본과 같은 막대형 인쇄 배치는 아니며 자동 공정·법적 기한 산출도 아니다.
- 도급내역서: 이 서울시교육청용 XLSM의 전체 시트 목록에 없음. 외부 ZIP 구성원은 공사서류/설계용역서류/설계비산출 5개 XLSM뿐이며 도급내역서 전용 파일 없음. Windows 추출 파일명 `내역` 검색도 일치 없음. 추가로 보존 ZIP 62개 구성원명을 읽기 전용 조사(62 성공/0 실패)하여 `내역|산출|원가` 41개 일치(설계비 프로그램, 토목 제경비, 철재중량 등)를 확인했지만 도급내역서 명칭은 식별하지 못했다. 중첩 ZIP 내부·모든 XLSM 시트·별도 사용자 자료까지 조사 완료한 것은 아니다.
- 읽기 전용 .NET ZIP/XML 분석, UTF-8 출력 적용. 원본 변경·매크로 실행·새 웹 수집 없음. 개인정보/운영 데이터는 외부 모델에 보내지 않음.

사용자가 Mac 환경에서 실제 한글 검증을 건너뛰고 후속 작업을 지시했다. native 검증은 미검증으로 유지하며 진행 차단 조건으로 삼지 않는다.

같은 원본 ZIP의 서울시교육청용 XLSM을 읽기 전용으로 분석했다. `13.착공계,공정표,대리인계(간이)` 복합 서식 대신 단독 착공계 시트를 사용한다.

| 서식 | 원본 시트 / XML | 주요 대응 |
| --- | --- | --- |
| 착공계 | `14.착공계` / `sheet18.xml`, A1:T31 | D4 공사명, C6 계약금액, C8 계약일, C10 실제 착공일, C12 준공기한, C14:C16 붙임, A21 제출일, E23:E26 업체 정보, B29 수신인 |
| 준공계 | `24.준공계` / `sheet28.xml`, A1:U32 | C4 공사명, C6 금액, C8 계약일, C10 실제 착공일, C12 기한, C14 실제 준공일, A20 제출일, F23:F26 업체 정보, B29 수신인 |
| 대금청구서 | `30.대금청구서` / `sheet35.xml`, A1:R34 | C4 공사명, C6 계약금액, C8 준공금액, C10 기지급액, C12 청구금액, C14 공제액, D20:D22 은행/계좌/예금주, D24 날짜, E26:E28 업체, B32 수신인 |

원본과 의도적으로 다르게 처리하는 항목:
- 원본 착공/준공계 제출일은 실제 착공/준공일을 참조한다. 서비스에서는 제출일을 별도로 입력하며 실제 날짜를 추정하지 않는다. 실제 준공일이 계약상 기한 이후인 경우도 기록 가능하다(지체 여부/금액 판단 아님).
- 대금청구 원본 C12는 `계약금액 - 기지급액 - 공제액` 수식이고 기지급/공제는 다른 장부/정산 시트에 의존한다. 장부·정산 연동을 아직 구현하지 않았으므로 모든 금액을 사용자 검토 후 직접 입력한다. 준공금액을 계약금액으로 대체하거나 수식을 임의 변경/승인하지 않는다.
- 원본 예금주는 업체명, 청구 날짜는 준공검사일을 참조한다. 서비스에서는 예금주와 청구일을 따로 입력한다. 실제 이체·지급·프로젝트 상태 전이는 실행하지 않는다.
- 원본 기관명 끝글자를 이용한 수신 직위 자동 선택은 가져오지 않고 수신인 직위를 직접 입력한다.
- 원본 착공계 기본 붙임 3종은 자동 첨부/생성하지 않는다. 현장대리인계·예정공정표·도급내역서를 사용자가 준비한 경우만 목록에 기재한다.
- 원 단위 숫자는 정확한 문자열로 출력하며 Excel NUMBERSTRING의 한글 금액 병기는 이번에 구현하지 않는다.
- 33개 기존 공통/계약 항목에 후속 전용 14개를 추가한다. 서식별 화면·문서·내부 미리보기는 해당 항목만 포함한다. 은행/계좌/예금주는 대금청구서에만 출력한다.
- 저장 형식 버전 v2를 추가하되 v1을 계속 읽고 표준계약서 출력도 지원한다. 기존 저장 revision은 바꾸지 않고 응답에서 없는 추가 항목을 빈 값으로 보충한다.
